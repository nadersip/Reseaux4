- name: Updating servers and installing services
  hosts: rackservers
  become: yes

  vars:
    ansible_become_pass: "" #admin password
  tasks:
   - name: Servers update
     shell: sudo apt update && sudo apt upgrade -y
   
   - name: Apache2 installation
     apt:
      name: apache2
      state: present

   - name: Dnsmasq installation
     apt: 
      name: dnsmasq
      state: present

   - name : Chaning the directory cisco permission 
     file:
      path: /home/cisco
      state: directory
      mode: '0755'

   - name : Creating the hostnames.txt file for dnsmasq
     file:
      path: /home/cisco/hostnames.txt
      state: touch
      mode: '0666'

   - name : Disable systemd-resolved service
     systemd:
      name: systemd-resolved
      state: stopped
      enabled: false
     become: yes

   - name : Dnsmasq enable service
     systemd:
      name: dnsmasq
      state: restarted
      enabled: true
     become: yes

   - name: Chrony installation
     apt: 
      name: chrony
      state: present
      
   - name: TFTP installation
     apt: 
      name: tftpd-hpa
      state: present

   - name: DHCP installation
     apt: 
      name: isc-dhcp-server
      state: present

   - name: Syslog installation
     apt: 
      name: rsyslog 
      state: present

   - name: openbsd-inetd installation
     apt: 
      name: openbsd-inetd 
      state: present

   - name: telnetd installation
     apt: 
      name: telnetd 
      state: present

- name: Config for server Rack-A
  hosts: rack_a
  become: yes
  vars:
    ansible_become_pass: "" #admin password
  tasks:
    - name : Get the html and css code for GitHub
      get_url:
        url: "{{ item.url }}"
        dest: /var/www/html/
      loop:
        - { url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_a/index.html"}
        - { url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_a/style.css"}
    - name : Get the dnsmasq config file
      get_url:
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_a/dnsmasq-config"
        dest: /etc/dnsmasq.d/rack-a.local
    - name: Get hostnames.txt file
      get_url: 
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_a/hostnames.txt"
        dest: /home/cisco/hostnames.txt
    - name : Restarting Dnsmasq service
      systemd:
        name: dnsmasq
        state: restarted
        enabled: true
      become: yes
    - name : Get the dhcp pool config file
      get_url:
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_a/dhcpd.conf"
        dest: /etc/dhcp/dhcpd.conf
    - name : Set the dhcp interface
      get_url:
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_a/isc-dhcp-server"
        dest: /etc/default/isc-dhcp-server
    - name : Restarting dhcp service
      systemd:
        name: isc-dhcp-server
        state: restarted
        enabled: true

- name: Config for server Rack-B
  hosts: rack_b
  become: yes
  vars:
    ansible_become_pass: "" #admin password
  tasks:
    - name : Get the html and css code for GitHub
      get_url:
        url: "{{ item.url }}"
        dest: /var/www/html/
      loop:
        - { url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_b/index.html"}
        - { url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_b/style.css"}
    - name : Get the dnsmasq config file
      get_url:
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_b/dnsmasq-config"
        dest: /etc/dnsmasq.d/rack-B.local
    - name: Get hostnames.txt file
      get_url: 
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_b/hostnames.txt"
        dest: /home/cisco/hostnames.txt
    - name : Restarting Dnsmasq service
      systemd:
        name: dnsmasq
        state: restarted
        enabled: true
      become: yes
    - name : Get the dhcp pool config file
      get_url:
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_b/dhcpd.conf"
        dest: /etc/dhcp/dhcpd.conf
    - name : Set the dhcp interface
      get_url:
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_b/isc-dhcp-server"
        dest: /etc/default/isc-dhcp-server 
    - name : Restarting dhcp service
      systemd:
        name: isc-dhcp-server
        state: restarted
        enabled: true

- name: Config for server Rack-C
  hosts: rack_c
  become: yes
  vars:
    ansible_become_pass: "" #admin password
  tasks:
    - name : Get the html and css code for GitHub
      get_url:
        url: "{{ item.url }}"
        dest: /var/www/html/
      loop:
        - { url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_c/index.html"}
        - { url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_c/style.css"}
    - name : Get the dnsmasq config file
      get_url:
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_c/dnsmasq-config"
        dest: /etc/dnsmasq.d/rack-c.local
    - name: Get hostnames.txt file
      get_url: 
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_c/hostnames.txt"
        dest: /home/cisco/hostnames.txt
    - name : Restarting Dnsmasq service
      systemd:
        name: dnsmasq
        state: restarted
        enabled: true
      become: yes
    - name : Get the dhcp pool config file
      get_url:
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_c/dhcpd.conf"
        dest: /etc/dhcp/dhcpd.conf
    - name : Set the dhcp interface
      get_url:
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_c/isc-dhcp-server"
        dest: /etc/default/isc-dhcp-server
    - name : Restarting dhcp service
      systemd:
        name: isc-dhcp-server
        state: restarted
        enabled: true

- name: Config for server Rack-D
  hosts: rack_d
  become: yes
  vars:
    ansible_become_pass: "" #admin password
  tasks:
    - name : Get the html and css code for GitHub
      get_url:
        url: "{{ item.url }}"
        dest: /var/www/html/
      loop:
        - { url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_d/index.html"}
        - { url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_d/style.css"}
    - name : Get the dnsmasq config file
      get_url:
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_d/dnsmasq-config"
        dest: /etc/dnsmasq.d/rack-d.local
    - name: Get hostnames.txt file
      get_url: 
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_d/hostnames.txt"
        dest: /home/cisco/hostnames.txt
    - name : Restarting Dnsmasq service
      systemd:
        name: dnsmasq
        state: restarted
        enabled: true
      become: yes
    - name : Get the dhcp pool config file
      get_url:
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_d/dhcpd.conf"
        dest: /etc/dhcp/dhcpd.conf
    - name : Set the dhcp interface
      get_url:
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_d/isc-dhcp-server"
        dest: /etc/default/isc-dhcp-server
    - name : Restarting dhcp service
      systemd:
        name: isc-dhcp-server
        state: restarted
        enabled: true         

- name: Config for server Rack-E
  hosts: rack_e
  become: yes
  vars:
    ansible_become_pass: "" #admin password
  tasks:
    - name : Get the html and css code for GitHub
      get_url:
        url: "{{ item.url }}"
        dest: /var/www/html/
      loop:
        - { url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_e/index.html"}
        - { url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_e/style.css"}
    - name : Get the dnsmasq config file
      get_url:
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_e/dnsmasq-config"
        dest: /etc/dnsmasq.d/rack-e.local
    - name: Get hostnames.txt file
      get_url: 
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_e/hostnames.txt"
        dest: /home/cisco/hostnames.txt
    - name : Restarting Dnsmasq service
      systemd:
        name: dnsmasq
        state: restarted
        enabled: true
      become: yes
    - name : Get the dhcp pool config file
      get_url:
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_e/dhcpd.conf"
        dest: /etc/dhcp/dhcpd.conf
    - name : Set the dhcp interface
      get_url:
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_e/isc-dhcp-server"
        dest: /etc/default/isc-dhcp-server
    - name : Restarting dhcp service
      systemd:
        name: isc-dhcp-server
        state: restarted
        enabled: true

- name: Config for server Rack-F
  hosts: rack_f
  become: yes
  vars:
    ansible_become_pass: "" #admin password
  tasks:
    - name : Get the html and css code for GitHub
      get_url:
        url: "{{ item.url }}"
        dest: /var/www/html/
      loop:
        - { url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_f/index.html"}
        - { url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_f/style.css"}
    - name : Get the dnsmasq config file
      get_url:
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_f/dnsmasq-config"
        dest: /etc/dnsmasq.d/rack-f.local
    - name: Get hostnames.txt file
      get_url: 
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_f/hostnames.txt"
        dest: /home/cisco/hostnames.txt
    - name : Restarting Dnsmasq service
      systemd:
        name: dnsmasq
        state: restarted
        enabled: true
      become: yes
    - name : Get the dhcp pool config file
      get_url:
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_f/dhcpd.conf"
        dest: /etc/dhcp/dhcpd.conf
    - name : Set the dhcp interface
      get_url:
        url: "https://raw.githubusercontent.com/nadersip/Reseaux4/refs/heads/main/servers_config/rack_f/isc-dhcp-server"
        dest: /etc/default/isc-dhcp-server
    - name : Restarting dhcp service
      systemd:
        name: isc-dhcp-server
        state: restarted
        enabled: true